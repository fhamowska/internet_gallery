{% extends 'base_admin.twig' %}

{% block title %}
    Edit Genre
{% endblock %}

{% block content %}
    <div class="container container-edit mt-5 pl-5 pr-5">
        <h1 class="mb-4 mt-4 pt-5 ml-3 pl-4">Edytuj kategorię</h1>

        {% if error %}
            <div class="alert alert-danger" role="alert">
                {{ error }}
            </div>
        {% endif %}
        <form action="genre_edit.php" method="post">
            <input type="hidden" name="id" value="{{ genre.id }}">
            <div class="form-group">
                <label for="name">Nazwa:</label>
                <input type="text" class="form-control" id="name" name="name" value="{{ genre.name }}" required>
            </div>
            <button type="submit" class="btn btn-primary">Zapisz</button>
        </form>
    </div>
{% endblock %}
