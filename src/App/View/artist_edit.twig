{% extends 'base_admin.twig' %}

{% block title %}
    Edytuj artystę
{% endblock %}

{% block content %}
    <div class="container container-edit mt-5 pl-5 pr-5">
        <div class="mx-0">
            <div class="breadcrumb my-breadcrumb pt-3 px-0">
                <a class="my-a me-3 mx-3" href="artists_admin.php">Lista artystów</a><span>></span><div class="ml-3"> Edytuj artystę</div>
            </div>
        </div>
        <h1 class="mb-3 mt-4 ml-3 pl-4">Edytuj artystę</h1>

        {% if error %}
            <div class="alert alert-danger" role="alert">
                {{ error }}
            </div>
        {% endif %}

        <form action="artist_edit.php" method="post">
            <input type="hidden" name="id" value="{{ artist.id }}">
            <div class="form-group">
                <label for="first_name">Imię:</label>
                <input type="text" class="form-control border border-dark" name="first_name" id="first_name" value="{{ artist.first_name }}" required>
            </div>

            <div class="form-group">
                <label for="last_name">Nazwisko:</label>
                <input type="text" class="form-control border border-dark" name="last_name" id="last_name" value="{{ artist.last_name }}" required>
            </div>

            <div class="form-group">
                <label for="year_of_birth">Rok urodzenia:</label>
                <input type="number" class="form-control border border-dark" name="year_of_birth" id="year_of_birth" value="{{ artist.year_of_birth }}" required>
            </div>

            <div class="form-group">
                <label for="year_of_death">Rok śmierci:</label>
                <input type="number" class="form-control border border-dark" name="year_of_death" id="year_of_death" {% if artist.year_of_death != 0 %} value="{{ artist.year_of_death }}" {% endif %}>
            </div>

            <button type="submit" class="btn btn-primary">Zapisz zmiany</button>
        </form>
    </div>
{% endblock %}
